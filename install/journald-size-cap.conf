# journald has no per-unit size cap, so this caps the whole system journal.
# Reasonable for a single-purpose kiosk device where METARBoard is the only
# substantial service running. Install to /etc/systemd/journald.conf.d/ and
# run `systemctl restart systemd-journald` to apply.
[Journal]
SystemMaxUse=200M
